

env = Environment()

srcFiles = [
  'TicTacToeBoard.cc',
  'TicTacToePlayer.cc',
  'TicTacToePlayerStrategies.cc',
  'ticTacToe.cc'
]

env.Append(CCFLAGS='-g')
env.Program(target='ticTacToe', source=srcFiles)
